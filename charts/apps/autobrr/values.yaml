---
image:
  repository: ghcr.io/autobrr/autobrr
  tag: latest
  pullPolicy: IfNotPresent

env:
  TZ: Europe/London

persistence:
  config:
    enabled: true

service:
  main:
    ports:
      http:
        port: 7474
