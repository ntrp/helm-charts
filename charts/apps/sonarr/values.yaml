---
image:
  repository: lscr.io/linuxserver/sonarr
  tag: 3.0.9.1549-ls164
  pullPolicy: IfNotPresent

env:
  TZ: Europe/London

persistence:
  config:
    enabled: true

service:
  main:
    ports:
      http:
        port: 8989
